[@(one:foo/trnsL), @(two/t), @(three/L)]
  escape\\ tab\t return\r newline\n space\s.\
  same line.
  | - one
  escape\\ tab\t return\r newline\n space\s.\
  same line.
  |-two
  \f\i\r\s\t\\
  second
  |-three

